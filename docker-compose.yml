version: "3.8"

services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    depends_on:
      - migrate

  migrate:
    build: .
    command: python -m migrations.run_migrations
    env_file: .env
    restart: "no"
