# SSH connection to the server
DEPLOY_HOST=your-server-ip
DEPLOY_USER=tginvitebot
SSH_KEY=~/.ssh/id_ed25519

# Where the bot lives on the server
DEPLOY_DIR=/home/tginvitebot/TgInviteBot

# systemd service name (managed via sudo)
SERVICE_NAME=tgbot
